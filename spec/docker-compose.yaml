services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    ports:
      - 80:8080
    environment:
      - SWAGGER_JSON_URL=http://localhost/spec/openapi.yaml
    volumes:
      - ./:/usr/share/nginx/html/spec
